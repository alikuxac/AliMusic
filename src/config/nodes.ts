export const nodes: nodesOptions[] = [
    {
        host: "n17.danbot.host",
        port: 1496,
        password: "alikuxac"
    }
];

interface nodesOptions {
    host: string,
    port: number,
    password: string
}